targets=call0,callf,callfn,calln,calls,includes,tests.log
cppflags_force=-I../include
cflags_force=`pkg-config --cflags libSystem`
cflags=-W -Wall -g -O2 -pedantic -fPIE -D_FORTIFY_SOURCE=2 -fstack-protector-all
ldflags_force=`pkg-config --libs libSystem` $(OBJDIR)../src/libMarshall.a
ldflags=-pie -Wl,-z,relro -Wl,-z,now
dist=Makefile,pkgconfig.sh,tests.sh

[call0]
type=binary
sources=call0.c

[callf]
type=binary
sources=callf.c

[callfn]
type=binary
sources=callfn.c

[calln]
type=binary
sources=calln.c

[calls]
type=binary
sources=calls.c

[includes]
type=binary
sources=includes.c

[tests.log]
type=script
script=./tests.sh
depends=pkgconfig.sh,tests.sh,$(OBJDIR)../src/libMarshall.a,$(OBJDIR)call0$(EXEEXT),$(OBJDIR)callf$(EXEEXT),$(OBJDIR)callfn$(EXEEXT),$(OBJDIR)calln$(EXEEXT),$(OBJDIR)calls$(EXEEXT),$(OBJDIR)includes$(EXEEXT)
